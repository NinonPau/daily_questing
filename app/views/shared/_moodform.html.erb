<div class="moods-card">
  <ul>
    <li>
      <i class="fa-solid fa-face-smile-wink"></i> Amazing
      <%= simple_form_for @user_mood do |f| %>
        <%= f.hidden_field :mood_type, value: "Amazing" %>
        <%= button_tag type: "submit", class: "btn liquid" do %>
          <span>Select</span>
        <% end %>
      <% end %>
    </li>

    <li>
      <i class="fa-solid fa-face-smile-wink"></i> Good
      <%= simple_form_for @user_mood do |f| %>
        <%= f.hidden_field :mood_type, value: "Good" %>
        <%= button_tag type: "submit", class: "btn liquid" do %>
          <span>Select</span>
        <% end %>
      <% end %>
    </li>

    <li>
      <i class="fa-solid fa-face-smile-wink"></i> Ok'ish
      <%= simple_form_for @user_mood do |f| %>
        <%= f.hidden_field :mood_type, value: "Ok'ish" %>
        <%= button_tag type: "submit", class: "btn liquid" do %>
          <span>Select</span>
        <% end %>
      <% end %>
    </li>

    <li>
      <i class="fa-solid fa-face-smile-wink"></i> Bad
      <%= simple_form_for @user_mood do |f| %>
        <%= f.hidden_field :mood_type, value: "Bad" %>
        <%= button_tag type: "submit", class: "btn liquid" do %>
          <span>Select</span>
        <% end %>
      <% end %>
    </li>
  </ul>
</div>

<div class="mb-3 back-link">
  <%= link_to root_path, class: "btn liquid" do %>
    <span>Back</span>
  <% end %>
</div>

