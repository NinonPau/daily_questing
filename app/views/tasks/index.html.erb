<div class="container">
<div class="row justify-content-center">
<div class="col-md-6">
<h1> Quests</h1>

<div class= "task">
  <% @tasks.each do |task| %>
    <p><strong>Task:</strong> <%= task.name %></p>
    <p><%= task.user_task.complited ? "✔" : "❌" %></p>
    <p><%= task.description %></p>
  <% end %>
  </div>
<div class="button">
<%= link_to "Main Menu", root_path, class: "btn btn-primary" %>
</div>
</div>
</div>
</div>
