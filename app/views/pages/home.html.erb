<% if user_signed_in? %>
  <!-- Dashboard Actions -->
  <div class="d-flex justify-content-center flex-wrap gap-3 my-4">
    <% if current_user.user_mood %>
      <%= link_to raw('<i class="fa-solid fa-cloud-sun btn-icons"></i> Update Mood'), edit_user_mood_path(current_user.user_mood), class: "btn btn-warning btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;"  %>
    <% else %>
      <%= link_to "Log Mood", new_user_mood_path, class: "btn btn-warning btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;"  %>
    <% end %>
    <%= link_to raw('<i class="fa-solid fa-scroll btn-icons"></i> View Quests'), tasks_path, class: "btn btn-success btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;" %>
    <%= link_to raw('<i class="fa-solid fa-feather btn-icons"></i> Add Quest'), new_task_path, class: "btn btn-primary btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;" %>
    <%= simple_form_for :task, url: random_tasks_path do |f| %>
      <%= f.submit 'Add Random Quest', class: "btn btn-pink btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;" %>
    <% end %> <!-- Ask teacher about icon in submit, knowing that raw() is being escaped -->

    <!-- New Friendship Button -->
    <%= link_to raw('<i class="fa-solid fa-handshake-angle btn-icons"></i> See Questers'), friendships_path, class: "btn btn-info btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;" %>

    <!-- new button Chat -->
    <%= link_to raw('<i class="fa-solid fa-fire btn-icons"></i> Go to Hearth'), chat_rooms_path, class: "btn btn-info btn-lg shadow-sm", style: "border-radius: 15px; min-width: 180px;" %>
  </div>


<% else %>
  <%= render "public_home" %>
<% end %>
